\documentclass[./main.tex]{subfiles}

% --------------------------------------------------------------------------------
\begin{document}
% --------------------------------------------------------------------------------
\begin{titlingpage}
  % Double-Boxed Border
  \begin{tikzpicture}[remember picture, overlay]
    % Lengths
    \newcommand*{\inset}      % inset from page edges
    {0.1\paperwidth}
    \newcommand*{\borderSep}  % seperation between borders
    {7.5pt}
    \newcommand*{\edgeSep}    % gap between border edges
    {2.5pt}

    % Co-ordinates
    %% Outer
    \coordinate (NWO) at ($(current page.north west) + (\inset,-\inset)$);
    \coordinate (NEO) at ($(current page.north east) + (-\inset,-\inset)$);
    \coordinate (SWO) at ($(current page.south west) + (\inset,\inset)$);
    \coordinate (SEO) at ($(current page.south east) + (-\inset,\inset)$);
    
    %% Inner
    \coordinate (NWI) at ($(NWO) + (\borderSep,-\borderSep)$);
    \coordinate (NEI) at ($(NEO) + (-\borderSep,-\borderSep)$);
    \coordinate (SWI) at ($(SWO) + (\borderSep,\borderSep)$);
    \coordinate (SEI) at ($(SEO) + (-\borderSep,\borderSep)$);
    
    % Borders
    %% Outer
    \draw[line width = 1.6pt, MITred]
      ($(NWO) + (\edgeSep,0)$)  -- ($(NEO) + (-\edgeSep,0)$)
      ($(SWO) + (\edgeSep,0)$)  -- ($(SEO) + (-\edgeSep,0)$)
      ($(NWO) + (0,-\edgeSep)$) -- ($(SWO) + (0,\edgeSep)$)
      ($(NEO) + (0,-\edgeSep)$) -- ($(SEO) + (0,\edgeSep)$);
    
    %% Inner
    \draw[line width = 1.6pt, MITgray]
      ($(NWI) + (\edgeSep,0)$)  -- ($(NEI) + (-\edgeSep,0)$)
      ($(SWI) + (\edgeSep,0)$)  -- ($(SEI) + (-\edgeSep,0)$)
      ($(NWI) + (0,-\edgeSep)$) -- ($(SWI) + (0,\edgeSep)$)
      ($(NEI) + (0,-\edgeSep)$) -- ($(SEI) + (0,\edgeSep)$);
  \end{tikzpicture}
  
  \centering
  
  \scshape \LARGE \color{MITgray}
  Single-Variable
  
  \LARGE \upshape \bfseries
  CALCULUS

  \vspace*{1em}
  
  \color{MITred} 
  \rule{\textwidth}{1.6pt}
  
  \upshape \bfseries \Huge
  PRINCIPIA

  \LARGE
  MATHEMATICA

  \rule{\textwidth}{1.6pt}

  \Huge
  \vspace*{2em}

  \color{MITgray} \mdseries \scshape \Large
  Syed, Danial Haseeb

  \vfill

  \rule{\textwidth}{0.8pt}

  \normalsize
  Powered By
  
  \vspace*{1em}

  \color{MITred}
  OpenCourseWare

  \includegraphics[height = 1em]{images/MIT.pdf}
\end{titlingpage}
% --------------------------------------------------------------------------------
\end{document}